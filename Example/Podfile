use_frameworks!

target 'NuSignUp_Example' do
  pod 'NuSignUp', :path => '../'

  pod 'InputMask', '~> 4.1.1'#add mask to text on TextField

  target 'NuSignUp_Tests' do
    inherit! :search_paths

    
  end
end
